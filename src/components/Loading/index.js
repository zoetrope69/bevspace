import { h, Component } from 'preact';

export default class Loading extends Component {
  render() {
    return (
    <div style={{ padding: '1em' }}>
      <h1>Loading...</h1>
    </div>
    );
  }
}
